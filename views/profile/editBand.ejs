<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/profile/createMusician">Create Musician</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/profile/musicianList">Musician List</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/profile/createBand">Create Band</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/profile/bandList">Band List</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="/logout">Logout</a>
      </li>
    </ul>
  </div>
</div>
</nav>

<h1>Edit Band</h1>
<form
  action="/profile/editBand/<%- bandToUpdate._id %>"
  method="post"
  class="mb-3"
>
  <label for="name" class="form-label">Band Name:</label>
  <input
    type="text"
    id="name"
    name="name"
    value="<%- bandToUpdate.name %>"
    style="margin-bottom: 10px"
    class="form-control"
  /><br />

  <label for="members">Current Band Members:</label>
  <h5
    id="members"
    name="members"
    value="<%- bandToUpdate.members %>"
    style="margin-bottom: 10px"
  >
    <% bandToUpdate.members.forEach(element => { %>
    <option value="<%-element._id%>"><%-element.name%></option>
    <% }) %>
  </h5>
  <br />

  <label for="members" class="form-label">Potential Band Members:</label>
  <select
    id="members"
    name="members"
    style="margin-bottom: 10px"
    class="form-control"
  >
    <% allMembers.forEach(element => { %>
    <option value="<%-element._id%>"><%-element.name%></option>
    <% }) %>
  </select>
  <select
    id="members"
    name="members"
    style="margin-bottom: 10px"
    class="form-control"
  >
    <% allMembers.forEach(element => { %>
    <option value="<%-element._id%>"><%-element.name%></option>
    <% }) %>
  </select>
  <select
    id="members"
    name="members"
    style="margin-bottom: 10px"
    class="form-control"
  >
    <% allMembers.forEach(element => { %>
    <option value="<%-element._id%>"><%-element.name%></option>
    <% }) %>
  </select>
  <select
    id="members"
    name="members"
    style="margin-bottom: 10px"
    class="form-control"
  >
    <% allMembers.forEach(element => { %>
    <option value="<%-element._id%>"><%-element.name%></option>
    <% }) %>
  </select>
  <br />

  <label for="genre" class="form-label">Genre:</label>
  <input
    type="text"
    id="genre"
    name="genre"
    value="<%- bandToUpdate.genre %>"
    style="margin-bottom: 10px"
    class="form-control"
  /><br />

  <label for="dateFounded" class="form-label">Date Founded:</label>
  <input
    type="number"
    id="dateFounded"
    name="dateFounded"
    value="<%- bandToUpdate.dateFounded %>"
    style="margin-bottom: 10px"
    class="form-control"
  /><br />

  <button type="submit" class="btn btn-outline-light">Update Band</button>
</form>
